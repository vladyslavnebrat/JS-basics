function sayWelcome(userName, currentDay) {
  //const message = 'Hello ' + userName + '! Have a nice ' + currentDay + '!';
  //return message;
  return message = 'Hello ' + userName + '! Have a nice ' + currentDay + '!';
}

//const result = sayWelcome('Andrey', 'Monday');
//console.log(result);

const personName = 'Denis';
const currentDay = 'Monday';
const result = sayWelcome  (personName, currentDay);
console.log (result);

function sayWelcome(userName, currentDay) {
  console.log (message = 'Hello ' + userName + '! Have a nice ' + currentDay + '!');
}
 sayWelcome ('Vlad', Friday);
 sayWelcome ('Vlad', Thursday);
 sayWelcome ('Everyone', Monday);